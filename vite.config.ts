import { defineConfig } from "vite";

// base is "./" so the build works from any path, including a GitHub Pages
// project page like username.github.io/terminal-journal/
export default defineConfig({
  base: "./",
});
